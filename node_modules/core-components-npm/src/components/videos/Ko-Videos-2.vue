<template>
  <div class="wrapper-videos" :style="Settings">
    <div class="contenedor">
      <!-- contenedor izquierdo -->
      <div class="content-left">
        <div class="content-video">
          <div class="wrapper-video">
            <iframe
              :src="
                `https://www.youtube.com/embed/${setting.url}?rel=0&amp;showinfo=0`
              "
              frameborder="0"
              allow="autoplay; encrypted-media"
              allowfullscreen
              v-if="setting.type === 'Youtube'"
            ></iframe>
            <iframe
              :src="`https://player.vimeo.com/video/${setting.url}`"
              frameborder="0"
              webkitallowfullscreen
              mozallowfullscreen
              allowfullscreen
              v-else
            ></iframe>
          </div>
        </div>
        <div class="content-mobil">
          <p class="text-subtitulo2">{{ this.Settings["--description"] }}</p>
        </div>
      </div>

      <!-- contenedor derecho -->
      <div class="content-right">
        <div>
          <p class="text-titulo">{{ this.Settings["--title_1"] }}</p>
          <p class="text-subtitulo">{{ this.Settings["--description"] }}</p>
        </div>
        <div class="content-button">
          <button ref="colorBtn" class="btn">{{ this.Settings["--text_btn"] }}</button>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
export default {
  name: "Ko-Videos-2",
  props: {
    dataStore: Object,
    Settings: Object,
    setting: {
      type: Object,
      default() {
        return {
          url: "4E4Bl9NenyI",
          type: "Youtube"
        };
      }
    }
  },
  mounted() {
    this.setBackgroungHover();
  },
  data() {
    return {};
  },
  computed: {
    colorCSS() {
      return this.Settings["--color_background_btn"];
    }
  },
  methods: {
    setBackgroungHover() {
      let color = getComputedStyle(this.$refs.colorBtn).getPropertyValue(
        "--color_background_btn"
      );
      let colorArray = color.split(",");
      let colorInt = parseInt(colorArray[2]);
      if (colorInt > 75) {
        colorArray[2] = `${parseInt(colorArray[2]) - 10}%`;
        this.$refs.colorBtn.style.setProperty(
          "--btnhover",
          colorArray.join(",")
        );
      } else {
        colorArray[2] = `${parseInt(colorArray[2]) + 10}%`;
        this.$refs.colorBtn.style.setProperty(
          "--btnhover",
          colorArray.join(",")
        );
      }
    }
  },
  watch: {
    colorCSS(value) {
      let colorArray = value.split(",");
      let colorInt = parseInt(colorArray[2]);
      if (colorInt > 75) {
        colorArray[2] = `${parseInt(colorArray[2]) - 10}%`;
        this.$refs.colorBtn.style.setProperty(
          "--btnhover",
          colorArray.join(",")
        );
      } else {
        colorArray[2] = `${parseInt(colorArray[2]) + 10}%`;
        this.$refs.colorBtn.style.setProperty(
          "--btnhover",
          colorArray.join(",")
        );
      }
    }
  }
};
</script>

<style scoped>
div.wrapper-videos {
  --btnhover: #000000;
}
.wrapper-videos {
  display: flex;
  width: 100%;
  background-color: var(--background_color_1);
  justify-content: center;
  align-items: center;
  box-sizing: border-box;
}
.contenedor {
  display: flex;
  width: 100%;
  max-width: 1300px;
  flex-direction: row;
  align-items: center;
  justify-content: space-around;
}
.contenedor > div:nth-child(1) {
  flex: 1;
  max-width: 650px;
}
.contenedor > div:nth-child(2) {
  flex: 1;
  margin-left: 50px;
  justify-content: center;
  align-self: center;
}
.content-left {
  display: flex;
  width: 100%;
  flex-direction: column;
  align-self: end;
}
.text-titulo {
  font-size: 22px;
  font-weight: bold;
  font-stretch: normal;
  font-style: normal;
  line-height: 1.45;
  letter-spacing: normal;
  color: var(--color_text);
}
.text-subtitulo {
  font-size: 16px;
  font-weight: normal;
  font-stretch: normal;
  font-style: normal;
  line-height: 1.63;
  letter-spacing: normal;
  color: var(--color_subtext);
  margin-top: 20px;
}
.text-subtitulo2 {
  font-size: 16px;
  font-weight: normal;
  font-stretch: normal;
  font-style: normal;
  line-height: 1.63;
  letter-spacing: normal;
  color: var(--color_subtext);
  margin-top: 20px;
}
.content-button {
  margin-top: 20px;
}
.btn {
  color: var(--color_text_btn);
  border-radius: var(--radius_btn);
  border: solid 2px var(--color_border_btn);
  background-color: var(--color_background_btn);
  padding: 4px 14px;
  width: 120px;
  font-size: 16px;
  font-weight: bold;
  cursor: pointer;
  transition: all 200ms ease-in;
}
.btn:hover {
  background-color: var(--btnhover);
}
.content-right {
  display: flex;
  width: 100%;
  flex-direction: column;
  margin-top: -60px;
}
.content-video {
  display: flex;
  justify-content: flex-end;
  align-items: center;
}
.wrapper-video {
  position: relative;
  max-width: 630px;
  width: 630px;
  height: 500px;
  overflow: hidden;
}
.wrapper-video iframe,
.wrapper-video object,
.wrapper-video embed {
  width: 100%;
  height: 100%;
}
.content-mobil {
  display: none;
}
@media (max-width: 1164px) {
  .contenedor > div:nth-child(2) {
    margin-left: 80px;
  }
}
@media (max-width: 1120px) {
  .contenedor > div:nth-child(2) {
    margin-left: 60px;
  }
}
@media (max-width: 1080px) {
  .contenedor > div:nth-child(2) {
    margin-left: 40px;
  }
}

@media (max-width: 1000px) {
  .contenedor {
    display: flex;
    width: 100%;
    max-width: 1300px;
    flex-direction: column-reverse;
    align-items: center;
    justify-content: space-around;
    padding: 0px 30px;
  }
  .contenedor > div:nth-child(2) {
    flex: 2;
    justify-content: space-between;
    margin-left: 0px;
    padding: 0px;
  }
  .text-subtitulo {
    display: none;
  }
  .content-right {
    flex-direction: row;
    margin-top: 20px;
  }
  .content-video {
    display: flex;
    justify-content: initial;
    align-items: initial;
  }
  .wrapper-video {
    position: relative;
    max-width: 1000px;
    width: 1000px;
    height: 481px;
    overflow: hidden;
  }
  .content-left {
    display: flex;
    width: 100%;
    padding-top: 20px;
  }
  .content-mobil {
    display: initial;
    flex-direction: column;
    margin-top: 10px;
    margin-bottom: 10px;
  }
  .content-button {
    margin-top: 0px;
  }
}
</style>
